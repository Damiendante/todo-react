/** @jsx jsx */
import { css, jsx } from '@emotion/core';

const style = css({
    textAlign: 'center',
    display: 'block',
    padding: '40px'
});

function Footer() {
    return (
        <footer>
            
        </footer>
    );
}

export default Footer;
